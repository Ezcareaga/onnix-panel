# Database
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29.0
alembic>=1.13.0

# Auth
bcrypt==4.2.1

# Environment
python-dotenv>=1.0.0

# Web framework
fastapi>=0.115.0,<0.135.2
starlette>=0.40.0,<1.0.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.31   # subido de 0.0.9: CVSS 8.7 (osv 2026-07-20)

# Session
itsdangerous>=2.1.0

# Phone parsing
phonenumbers>=8.13.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-rerunfailures>=14.0
httpx>=0.27.0

# Bot AI + Scheduling
anthropic>=0.43.0
google-genai>=1.0.0
pgvector>=0.3.0
apscheduler>=3.10.0,<4.0

# Excel export (leads page)
openpyxl>=3.1.0,<4.0

# Pisos de seguridad para dependencias transitivas (auditoría 2026-07-20).
mako>=1.3.12        # Mako <1.3.12 (CVSS 8.7) — transitivo vía alembic
pygments>=2.20.0    # Pygments <2.20 (CVSS 6.8) — transitivo vía pytest
idna>=3.15          # idna <3.15 (CVSS 6.9) — transitivo vía httpx/anthropic
# NOTA: starlette (CVSS 7.5) NO subido — el fix es 1.3.1 pero fastapi lo fija <1.0.0.
#       Requiere subir fastapi también (breaking). Diferido a revisión manual.
