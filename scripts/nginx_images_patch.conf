    # Property images — served directly from filesystem
    location /images/ {
        alias /home/onnix/images/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
        try_files $uri =404;
    }
